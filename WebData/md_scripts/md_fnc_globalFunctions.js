/*
    script acts as a function library
*/
let scriptEntry = document.getElementById("ScriptEntry");
function insertScript(htmlCode) {
    if(htmlCode instanceof String) {
        scriptEntry.inne
    } else {
        console.log("[INSERT SCRIPT]: html code give isn't a string:'{$htmlCode}'");
    };
};
//#region md_scripts\functions
